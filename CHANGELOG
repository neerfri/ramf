* 0.1.2 *
 * Added more known simple classes that don't need conversion in FlexAnonymousObject#to_params
 * Added ReferenceTableUser#key_of(object), fix bug with PlaceHolder
 * Bug fix: ByteArray objects not counted in reference

* 0.1.1 *
 * added FlexAnonymousObject#to_params to convert it to hash recursively

* 0.1.0 *
 * parsing is now done inside RAMF (AMFObject.process)
 
* 0.0.1 *
 * Initial release.