* 0.1.2 *
 * transient members are now also removed from amf scope :only (fix in FlexClassTraits)

* 0.1.1 *
 * added FlexAnonymousObject#to_params to convert it to hash recursively

* 0.1.0 *
 * parsing is now done inside RAMF (AMFObject.process)
 
* 0.0.1 *
 * Initial release.